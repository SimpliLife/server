const app = require('../app')
const port = process.env.PORT || 3000

app.listen(port, () => {
  let text = `
    DiagnoAkses is running on ${port}
    Local : http://localhost:${port}/api
  `
  console.log(text)
})